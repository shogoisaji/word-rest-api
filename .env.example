# =============================================================================
# Rust Turso API - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment
# All variables marked as REQUIRED must be set for the application to start

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the HTTP server to listen on
# Default: 8080 (Cloud Run will override this with PORT environment variable)
# REQUIRED: No (defaults to 8080)
PORT=8080

# =============================================================================
# Database Configuration (Turso)
# =============================================================================

# Turso database URL - must start with 'libsql://' for remote or 'file:' for local
# Example: libsql://word-rest-api-db.turso.io
# For local development, you can use: file:local.db
# REQUIRED: Yes
TURSO_DATABASE_URL=libsql://word-rest-api-db.turso.io

# Turso authentication token - get this from your Turso dashboard
# For local file databases, this can be any non-empty string
# REQUIRED: Yes
TURSO_AUTH_TOKEN=your-auth-token

# =============================================================================
# Application Environment
# =============================================================================

# Application environment - affects logging and error handling
# Values: local, production, prod
# - local: Detailed error messages, debug logging
# - production/prod: Generic error messages, structured logging
# REQUIRED: No (defaults to 'local')
ENV=local

# =============================================================================
# Logging Configuration
# =============================================================================

# Rust logging level - controls verbosity of application logs
# Values: error, warn, info, debug, trace
# Recommended: info for production, debug for development
# REQUIRED: No (defaults to 'info')
RUST_LOG=info

# =============================================================================
# Cloud Run Specific (Production)
# =============================================================================
# These variables are typically set by Cloud Run automatically
# You don't need to set them locally, but they're documented here for reference

# PORT - Set automatically by Cloud Run (overrides the PORT above)
# GOOGLE_CLOUD_PROJECT - Your GCP project ID
# K_SERVICE - Cloud Run service name
# K_REVISION - Cloud Run revision name
# K_CONFIGURATION - Cloud Run configuration name

# =============================================================================
# Development Only
# =============================================================================
# These variables are only used during development and testing

# Request timeout in seconds (for development testing)
# REQUIRED: No (handled by middleware configuration)
# REQUEST_TIMEOUT=30